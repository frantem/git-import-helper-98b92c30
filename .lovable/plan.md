Нужно исправить не только интерфейс, но и причину в базе: у пользователя с номером `+375336582639` есть запись в `auth.users`, но нет строки в `public.profiles`. Поэтому `/settings` показывает пустые поля, сохранение через `.update()` не меняет ни одной строки, а корзина снова отправляет в настройки.

План:

1. Восстановить текущий профиль для номера `+375336582639`
   - Создать недостающую строку в `public.profiles` для пользователя `6100de55-a181-46a9-b01e-05623eef5c4b`.
   - Сразу записать туда телефон `+375336582639`, `phone_verified = true`, виртуальный email и `has_password = false`.
   - Если роль покупателя для этого пользователя отсутствует — добавить `buyer`.

2. Починить регистрацию/вход по телефону на будущее
   - В `verify-otp` оставить/усилить `upsert` профиля так, чтобы он гарантированно создавал профиль при каждом входе через телефон, даже если записи ещё нет.
   - Для уже существующего пользователя с виртуальным email тоже выполнять `upsert`, а не только `update`.

3. Починить сохранение `/settings`
   - Заменить сохранение профиля с `.update()` на `.upsert({ user_id, ... }, { onConflict: 'user_id' })`, чтобы данные не терялись, если строки профиля ещё нет.
   - При сохранении телефона после подтверждения тоже гарантировать, что профиль существует.
   - После сохранения перечитать профиль или использовать сохранённые значения, чтобы переход из корзины проверял актуальные `имя + телефон + адрес`.

4. Починить проверку корзины
   - Проверка `К оформлению` остаётся только по трём полям: имя, телефон, адрес доставки.
   - Если профиль отсутствует, пользователь идёт в `/settings?from=cart`; если поля есть — в `/checkout`.

5. Проверка после исправления
   - Проверить в базе, что профиль для `+375336582639` появился.
   - Проверить, что `/settings` больше не должен быть пустым для этого аккаунта, а после заполнения имени и адреса корзина пропускает дальше.