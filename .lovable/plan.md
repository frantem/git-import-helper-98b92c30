
## Что нужно сделать

### 1. Три страницы документов

Создать страницы по тем же лекалам, что и существующая `PrivacyPolicy.tsx` (Header + PageHeader «Назад» + карточка с текстом + BottomNavigation):

- `src/pages/PrivacyPolicy.tsx` — переписать содержимое дословно из `LOCUS_Политика_конфиденциальности.docx` (новые реквизиты оператора, дата 15 мая 2026).
- `src/pages/Oferta.tsx` — новый, маршрут `/oferta`, текст из `LOCUS_Публичная_оферта_покупателям.docx`.
- `src/pages/SellerTerms.tsx` — новый, маршрут `/seller-terms`, текст из `LOCUS_Договор_с_продавцами.docx`.

Стиль: контейнер `max-w-3xl` (~768px, близко к запрошенным 800px) с тем же типографическим оформлением, что и текущий Privacy: `text-sm leading-relaxed`, заголовки разделов `text-base font-bold`, списки `list-disc pl-5`. Никаких PDF, никакой лишней графики.

Зарегистрировать оба новых маршрута в `src/App.tsx` через `lazy(() => import(...))`.

### 2. Общий футер

Сейчас футер живёт прямо в `src/pages/Index.tsx`. Вынести его в `src/components/Footer.tsx` и подключить на всех публичных страницах (Index, Catalog, Product, Cart, Checkout, Profile, Auth, Orders, Favorites, SellerProfile, SellerApplication, PrivacyPolicy, Oferta, SellerTerms, LocalLanding).

Содержимое футера (одна строка, `text-[11px] text-muted-foreground`, по центру):

```
УНП: CE6154534 · Котович Артём Владимирович
© 2026 Locus · Политика конфиденциальности · Публичная оферта · Условия для продавцов
```

Все три документа — `<Link>` на `/privacy-policy`, `/oferta`, `/seller-terms`.

Важно: в новых документах указано «Котович Артём Владимирович, самозанятый, УНП CE6154534», а не «ООО ЛОКУСФУД». Согласие на смену реквизитов в футере спрошу у пользователя при реализации, по умолчанию беру данные из присланных документов.

### 3. Согласия под кнопками

Везде — мелкий серый текст (`text-[11px] text-muted-foreground`, `mt-2`/`mt-3`), ссылки с `target="_blank" rel="noopener noreferrer"` и подчёркиванием при ховере.

- **Checkout** (`src/pages/Checkout.tsx`): под кнопкой «Оформить заказ» внутри итогового блока — «Нажимая кнопку, вы соглашаетесь с [Публичной офертой](/oferta) и [Политикой конфиденциальности](/privacy-policy)».
- **Auth** (`src/pages/Auth.tsx`, режим `register`): под кнопкой «Зарегистрироваться» — «Регистрируясь, вы соглашаетесь с [Политикой конфиденциальности](/privacy-policy)».
- **SellerApplicationForm** (`src/components/SellerApplicationForm.tsx`): под кнопкой подачи заявки — «Подавая заявку, вы соглашаетесь с [Условиями для продавцов](/seller-terms) и [Политикой конфиденциальности](/privacy-policy)».

### Технические детали

- Тексты документов копирую дословно из .docx в JSX (разбиваю на `<section>` по нумерованным пунктам, как в текущем `PrivacyPolicy.tsx`).
- Обновить memory `mem://legal/footer-compliance` — заменить «ООО ЛОКУСФУД» на новые реквизиты и добавить три обязательные ссылки в футер.
- Обновить memory `mem://legal/privacy-policy` — новая дата вступления в силу и новые реквизиты оператора.
- SEO: добавить `<SEO />` теги на новые страницы (title/description), `noindex` не ставим — документы должны быть доступны поисковикам.
