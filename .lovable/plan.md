В `src/components/ProductImageLightbox.tsx` удалить блок боковых тап-зон (кнопки слева/справа по 25% ширины для prev/next). Листание останется только свайпами через Embla, а на десктопе — стрелки по бокам (отдельный блок, не трогаем).

Технически: удалить JSX-блок `{images.length > 1 && !zoomed && (<>...левая и правая button с w-1/4...</>)}` (примерно строки 137–152).