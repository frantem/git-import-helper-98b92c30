

## План: Удалить вкладку "Товары" из /admin/blocks

### Что удаляется

**Файл:** `src/pages/admin/AdminBlocks.tsx`

1. **Интерфейс `Product`** (строки 28-40) — больше не нужен
2. **Состояние `products`** (строка 65) — `useState<Product[]>([])`
3. **Загрузка товаров в `fetchData`** (строки 122-138) — запрос к `products` с join
4. **Функция `handleDeleteProduct`** (строки 287-301)
5. **TabsTrigger "Товары"** (строки 556-559)
6. **TabsContent "products"** (строки 918-956) — весь блок отображения товаров
7. **Сетка табов** — изменить `grid-cols-3` на `grid-cols-2` (строка 547)
8. **Неиспользуемый импорт `Package`** из lucide-react (строка 18)
9. **Неиспользуемые импорты** `formatPrice` и `BynSymbol` (если они больше нигде не используются в файле)

### Что остаётся
- Вкладка "Блоки" с управлением блоками главной страницы
- Вкладка "Категории" с SEO-полями
- Диалог добавления товаров в блоки (использует прямые запросы к Supabase, не зависит от удаляемого состояния)

Один файл, ~80 строк удалено.

