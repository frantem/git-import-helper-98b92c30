

## План: Добавить SEO-поля в форму редактирования категорий в /admin/blocks

### Проблема
SEO-поля (`seo_title`, `seo_description`) были добавлены только в отдельную страницу `/admin/categories`, которая не используется (перекидывает на главную). Реальное управление категориями происходит в `/admin/blocks` во вкладке "Категории", и там SEO-полей нет.

### Изменения

**Файл:** `src/pages/admin/AdminBlocks.tsx`

1. Добавить поля `seo_title` и `seo_description` в состояние `categoryForm` (строка 72-76)
2. Добавить эти поля в `handleEditCategory` (строка 239-247)
3. Добавить эти поля в `resetCategoryForm` (строка 249-253)
4. Включить `seo_title` и `seo_description` в объект `categoryData` в `handleSaveCategory` (строка 168-172)
5. Добавить секцию "SEO (необязательно)" с двумя полями Input в диалог редактирования категории (после строки 836, перед кнопкой "Сохранить")

Секция SEO будет выглядеть так же, как в `AdminCategories.tsx` — разделитель, заголовок "SEO (необязательно)", поля Title и Description.

Один файл, ~25 строк добавлено.

